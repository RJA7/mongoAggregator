{
  "name": "mongo-aggregator",
  "version": "0.0.1",
  "description": "Creates aggregation objects for get documents with relations from mongo db.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mongo",
    "aggregation"
  ],
  "author": "RJA",
  "license": "ISC",
  "dependencies": {
    "chai": "latest"
  }
}
